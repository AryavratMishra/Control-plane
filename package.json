{
  "name": "controlplane-ai-firewall",
  "version": "3.0.0",
  "private": true,
  "description": "ControlPlane — a model-agnostic AI Firewall for performance, cost and responsibility risk.",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test tests/*.test.js",
    "check": "node --check server.js && node --check server/controllers/evaluationController.js"
  },
  "engines": { "node": ">=20" }
}
